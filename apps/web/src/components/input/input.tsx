import { styled } from '@stitches/react';

export const Input = styled('input', {
  border: '$border solid',
  color: '$primaryText',
  padding: '$2',
  borderRadius: '$3',
  borderWidth: '$sm',
  width: '100%',
});
